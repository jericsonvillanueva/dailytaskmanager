import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dailytaskmanager',
  appName: 'daily task manager',
  webDir: 'dist'
};

export default config;
